# Telegram API 配置
# 从 https://my.telegram.org/apps 获取
TELEGRAM_API_ID=your_api_id
TELEGRAM_API_HASH=your_api_hash

# Bot 配置
# 版本号自动从 package.json 读取，无需手动设置
BOT_NAME=NexBot
# BOT_VERSION=1.0.1  # 已废弃，版本号自动从 package.json 读取

# 命令前缀（开发模式自动使用 !）
CMD_PREFIX=.

# 数据库配置
DB_PATH=./data/nexbot.db

# 日志配置
LOG_LEVEL=info
LOG_PATH=./logs

# 插件配置
PLUGINS_DIR=./plugins
PLUGIN_REGISTRY_URL=https://raw.githubusercontent.com/nexbot/plugins/main/registry.json

# 功能开关
ENABLE_SHELL_EXEC=true
SHELL_TIMEOUT=30000
MAX_OUTPUT_LENGTH=4000
